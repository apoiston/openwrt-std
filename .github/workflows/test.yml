name: Test

on:
  workflow_dispatch:
    inputs:
      enable_build_tools:
        description: 'Build tools'
        required: false
        type: boolean
        default: false

      enable_build_kernel:
        description: 'Build kernel'
        required: false
        type: boolean
        default: false

jobs:
  build:
    name: Build
    runs-on: ubuntu-26.04

    steps:
      - name: Set timezone
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          date +"%Y-%m-%d %H:%M:%S %z"

      - name: Show runner info
        run: |
          date +"%Y-%m-%d %H:%M:%S %z"
          lscpu | grep -E '^Architecture|^CPU\(s\):|^Model name'
          uname -sr
          lsb_release -a
          free -h
          df -Th

      - name: Checkout
        uses: actions/checkout@v7

      - name: Build
        uses: apoiston/actions@builder
        with:
          private_key: ${{ secrets.PRIVATE_KEY }}
          public_key: ${{ secrets.PUBLIC_KEY }}
          prepare_environment: ${{ secrets.PREPARE_ENVIRONMENT }}
          prepare_config: ${{ secrets.PREPARE_CONFIG }}
          export_image: ${{ secrets.EXPORT_IMAGE }}
          config_buildinfo: builder/config.buildinfo
          enable_build_tools: ${{ inputs.enable_build_tools == true && 'true' || 'false' }}
          enable_build_kernel: ${{ inputs.enable_build_kernel == true && 'true' || 'false' }}
          enable_export_image: true

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          name: image
          path: |
            openwrt/builder/
            openwrt/builder-extra/
            openwrt/builder-kmods/
            openwrt/builder-packages/
